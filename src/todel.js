
	// 	const id = this._cmpId,
	// 		cmpLoaded = id === currCmp.id,
// 		    cmp = cmpLoaded ? currCmp : UIlocalStorage.get( id ),
// 			name = cmp.name || "untitled";

// 			if ( !this._wavReady ) {
// 				this._wavReady = 1;
// 				a.download = name + ".wav";
// 				gs.exportCurrentCompositionToWAV().then( url => {
// 					this._wavReady = 2;
// 					a.href = url;
// 				} );
// 			}
